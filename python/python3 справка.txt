динамическая типизация. т.е. при объявлений переменной нужно сразу давать ей значение.
потом питон сам определяет какого она типа.


ВЫВОД ТЕКСТА:
print("hello world") //печатать можно только строчки
в python2: print "hello world"
т.к. в python2 принт это оператор, а в 3 это функция.
оператор это арифметические и логические символы

ПЕРЕМЕННЫЕ:
a = 25
a = 35 //стирает старое и присваивает новое значение
b = "Hello"
c = (str(a) + " " + b) //печатать можно только строчки. если только номера, то преоброазование можно не делать
print (c)
print(1+2) // выведет 3, арифметика сразу доступна

a == b этот оператор проверяет равенство двух объектов
a is b проверяет идентичность самих объектов. Его используют, чтобы удостовериться, что переменные указывают на один и тот же объект в памяти



СТРОКИ:
name = "vasya pupkin"
c = "cats"[0] - ноль это индекс символа, в данном случае буква c
print(name.upper()) - функция для переменной name, все буквы большие
a = a.strip("r") //убрать в а все буквы r
print("rlgherj \n nreogn") //" \n это enter(новая строка); \t это tab"


ЦИКЛЫ:
for x in range (0,100):  //всё, что находится в цикле выполнится сто раз. название переменной любое, например х
    print("****")
    print(x)
#for x in range (0,100,2) //делать с шагом 2
  if x == 50: если x равен 50, то прерваться
    break
#здесь x = 50
x=0
while True:  //пока правда, x прибавляет 1
    print(x)
    x = x+1
    if x == 4:
        break


СПИСКИ(МАССИВЫ) (LISTS(ARRAY)):
это хранение данных с общим назначением
cities = ['Moscow', 'Riga', "Riga"]
print (cities)
print(len(cities)) //посчитат количество элементов
print(cities[0]) //напечатать первый элемент списка
print(cities[-1].upper()) //с конца списка распечатать первый и увеличить буквы
cities[2] = 'Tula' //изменить элемент
cities.append('Kursk') // добавить в конец
cities.insert(1, 'Kursk') //добавить на второе место
del cities[-1] // стереть элемент по номеру
cities.remove('Tula') // стереть по названию


ЦИКЛЫ НАД МАССИВАМИ:
cars = ['bmw', 'vw', 'seat', 'skoda']
for x in cars:
    print(x)   //вывести только названия, без скобок, запятых и кавычек и т.д.ы
number_list = list(range(0, 10)) //создать список с помощью цикла


class - класс
  def  - функция
  self.  //внутри метода обращается сам к себе
функция в классе называется метод
метод это:
	x = [1, 2, 3]
	x.pop()    в данном случае pop это метод
функция это тоже объект, сначала читается функция, потом по её результату создается объект

def f():
	print "test"
	g()
f()
1) в стек положится f()
2) положится принт, выполнится и удалится из стека
3) положится функция g(), выполнит все, что в ней есть и удалится из стека
3) f() выполнится и он удалится из стека

ДЕКОРАТОРЫ:
def 1(function_to_decorate)
	def 2
		function_to_decorate()
def 3
var = def1(def3) - передать третью функцию в первую

@def 1
def x - передать функцию в ту, в которой есть декоратор

примеры:
  def nape(name):
      print("Hello " + name)
  
  def summa(x, y):
      return x+y
  
  def factorial(x):
      otvet = x
      return otvet
	  
lst = [10, 20]
def (*lst) - все аргументы списка lst передать поочередно
      
  nape("1")
  print (summa(33, 22))
  print (factorial(3))

В блоке try мы выполняем инструкцию, которая может породить исключение, а в блоке except мы перехватываем их. При этом перехватываются как само исключение, так и его потомки. 
  
os.system('dir')

все переменные тоже объекты
test.append(5) - добавить для объекта текст
test.remove(2) - удалить
test.insert(0,4) - добавить на нулевой индекс(первое место в строке) цифру 4

функция max(test) - выведет максимальное число в списке
test.count(2) - сколько двоек в списке


print("%.2f" % total) - обрубить число после запятой(total) до двух знаков
'There\'s a snake in my boot!' - палка это экранирование, что бы строка не закрылась на кавычке например

достать значение json по ключу или python dict get value
простой data['key']
правильный data.get('key')
data[key] - распечатать все ключи

список ['с', 'п', 'и', 'с', 'о', 'к']
словарь {'dict': 1, 'dictionary': 2}
Словари в Python - неупорядоченные коллекции произвольных объектов с доступом по ключу. Их иногда ещё называют ассоциативными массивами или хеш-таблицами.
список содержит в себе словари вроде


prices = {"banana": 4,"apple": 2,"orange": 1.5,"pear": 3}

stock = {"banana": 6, "apple": 0, "orange": 32, "pear": 15}

for food in prices:
  print food
  print "price: %s" % prices[food]
  print "stock: %s" % stock[food]
  
  n[1] = n[1] * 5 - умножить второй элемент списка на 5
  n.pop(1) - удалить элемент списка по номеру
  n.remove(1) - удалить элемент списка по содержимому
  n[0] = n[0] * 2 - теперь нулевое элемонт раздвоится и их в списке будет два
